/*

We no longer require this file
// JS Object
export const initialState = {
    dishes: DISHES,
    comments: COMMENTS,
    promotions: PROMOTIONS,
    leaders: LEADERS
};

// Pure function - to generate the next state, we need current state and action
export const Reducer = (state = initialState, action) => {
    return state;
};*/
