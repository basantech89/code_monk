package mooc.vandy.java4android.calculator.logic;

public interface Computation {

    // Perform the operation, all the classes
    // must implements this interface
    String compute(int operation);
}
